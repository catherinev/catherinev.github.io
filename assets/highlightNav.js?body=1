$(document).ready(function(){
  var currentUrl = document.URL;
  console.log("hello");
  var regex = /(.+?)\?(.+)/;
  var baseUrl = currentUrl.replace(regex, "$1")
  console.log(baseUrl)
  $('a[href="' + baseUrl + '"]').addClass("current")
});
